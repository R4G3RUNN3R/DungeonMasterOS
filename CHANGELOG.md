# Changelog

## 2026-08-18 (Phase 6)
- In-game UI redesign: implemented Phase 6 (encumbrance/item integration) of the immersive live-play shell on `feature/immersive-ingame-ui-redesign`, per design spec §16 — the final phase of the redesign's original phased sequence.
- **Scope note:** the design spec gated this phase on "reconciling with the master item compendium/inventory work." That branch (`feature/master-item-compendium`) turned out to be a public, read-only item catalogue/browsing UI (cover flow, provenance, source links) — not a character-inventory/weight/carrying-capacity backend. Neither branch had any weight, container, or carrying-capacity data model, so this phase is new backend work rather than a merge, per explicit user decision after the gap was surfaced.
- `items` table: added `weight` (real, lbs per unit) and `carried` (bool, default true) columns. `carried` distinguishes items physically on the character from owned-but-stored assets (spec §16's carried-vs-owned requirement) — a house, a wagon, gear left at the guild hall — which correctly don't count toward carry weight.
- `shared/encumbrance.ts` (new): the real D&D 3.5e SRD Table 9-1 carrying-capacity-by-Strength lookup (exact for Str 1-20, approximated above via the standard "doubles every +10 Str" rule), a documented simplified level-derived fallback for characters with no recorded Strength score, tier classification (light/medium/heavy/overloaded), and the standard 3.5e load penalties (speed reduction, max Dex bonus cap, check penalty) per tier — mirroring the honest-fallback precedent set by Phase 4's combat-engine.ts.
- `client/src/lib/rulesAdapters/dnd35.ts`: the HUD's Carry Weight slot (spec §4.2/§16) has existed since Phase 1 as a hard-coded `null` — real data was explicitly deferred to this phase. Now derives Strength from `characterData.dnd35Sheet.abilities.str.score` when present, else the level-derived fallback, and computes real current/max/tier from the character's actual carried items.
- `CharacterHud.tsx`: the Carry stat now tints amber at heavy load and red at overloaded, matching the existing HP-bar color-by-threshold pattern.
- `InventoryOverlay.tsx`: each item shows its real per-unit weight; a capacity summary bar (current/max + tier label, tier-colored) sits in the header; stored (carried=false) items render muted with a "Stored" badge; a small Carry/Store toggle per item calls the real PATCH endpoint.
- `server/routes.ts`: item create/update routes accept `weight`/`carried`. The AI item-grant extractor (`extractItemsFromNarration`) now asks for a realistic per-unit weight, with a sane per-itemType fallback table if the AI omits one or returns something invalid — property/vehicles/mounts/creatures/retainers are correctly weightless (not carried).
- Deferred, honestly: container and Bag-of-Holding-specific capacity logic — spec §16 explicitly calls this out as needing its own separate logic rather than folding it into base encumbrance, so it wasn't built here.
- Verification: typecheck/build pass clean. Live-verified end-to-end through the real API (registered a user, created a campaign and a level-5 Fighter with a real Strength 16 in the 3.5e sheet, created items via the real item-creation endpoint): HUD showed "CARRY 78/230" (78 = 8+30+40 lbs of carried gear, correctly excluding a 12lb stored item; 230 = the real STR-16 heavy-load threshold from the SRD table), Inventory overlay showed the same 78/230 · Medium Load summary with correct per-item weights and a Stored badge on the excluded item. Clicked the Carry toggle on the stored item — both the HUD and overlay updated to 90/230 in real time via the real PATCH endpoint and its existing `items_updated` WebSocket broadcast. Added a 200lb item to push total to 290/230 and confirmed the tier flipped to "Overloaded" with `getComputedStyle` confirming the HUD stat actually rendered in the danger-red color (not just the right CSS class name).
- Risk: low-to-moderate. New columns are additive (`addColumnIfMissing`, safe on existing databases); the carrying-capacity math is isolated in a pure, unit-testable module; the HUD/Inventory changes only populate a slot that was always rendering "—" before, so there's no working behavior to regress.

## 2026-08-18 (Phase 4)
- In-game UI redesign: implemented Phase 4 (combat UX) of the immersive live-play shell on `feature/immersive-ingame-ui-redesign`, per design spec §10, with the scope explicitly upgraded from a cosmetic-only stub to a real working combat backend per user decision — this branch had zero combat mechanics (no encounters, no combatants, no attack resolution) before this phase.
- **Scope boundary (intentional, documented in-file):** this branch's `characters` table has no ability scores, AC, or BAB — only name/race/class/level/hp/speed/attacksPerRound. Rather than porting the full 3.5e mechanical substrate, `server/combat-engine.ts` uses simplified level-derived defaults (`defaultAttackBonusForLevel`, `defaultArmorClassForLevel`) so combat is real and playable today without silently pretending to be canonical 3.5e math it isn't.
- Schema: new `encounters` and `combatants` tables (`shared/schema.ts`, migrated via `runMigrations()` in `server/storage.ts` following the existing raw-SQL `CREATE TABLE IF NOT EXISTS` pattern) plus matching `IStorage`/`DatabaseStorage` methods.
- `server/combat-engine.ts` (new): d20 attack resolution with crit-on-20/fumble-on-1, damage dice parsing, level-derived stat defaults.
- `server/dm-engine.ts`: DM system prompt now documents `[COMBAT_START]{...}[/COMBAT_START]` and `[SURRENDER]{"name":"..."}[/SURRENDER]` output formats, explicit that the AI proposes combat/surrender but never decides hit, damage, or outcome — the server resolves every roll. Added `narrateCombatResult()` to have the AI narrate an already-decided mechanical result, with a graceful fallback to a plain mechanical summary if the AI call fails.
- `server/routes.ts`: combat orchestration — `startEncounterIfProposed`, `applySurrenderIfProposed`, `advanceCombatTurns` (auto-resolves NPC turns in a guarded loop until it's a player's turn or the encounter ends), `checkEncounterEndReason`. New endpoints: `GET /api/campaigns/:id/encounter`, `POST /api/campaigns/:id/combat/attack`, `POST /api/campaigns/:id/combat/flee` (all turn-limited and gated the same way as the main action endpoint).
- Client: `CombatContext.tsx` (round/turn-order/HP-bar Context Panel view), `ContextActionDeck.tsx` extended with dynamic per-target Attack buttons + Flee, `ContextPanel.tsx` gives Combat top priority over Merchant/Loot/Exploration per spec §8.3, `campaign.tsx`/`CampaignGameShell.tsx` wired to the new encounter query, attack/flee mutations, and `combat_started`/`combat_updated`/`combat_ended` WebSocket events.
- **Bug fix (found during live verification):** `checkEncounterEndReason()` treated "all players fled" and "all players defeated" identically (both reported `"defeat"`). Fixed to check `isDefeated` specifically so `endReason` is semantically correct for any future feature (achievements, campaign history) that reads it — not yet surfaced in the UI itself.
- Verification: since this dev environment has no `ANTHROPIC_API_KEY`, the AI-triggered `[COMBAT_START]` path couldn't fire naturally. Verified the entire real server stack end-to-end instead by seeding a genuine encounter + combatants directly into the running dev server's SQLite database, then exercising it live in the browser: Context Panel correctly showed round/turn/HP bars, dynamic Attack buttons per live NPC, a real server-resolved miss (`d20 roll 10 + attack bonus 1 = 11 vs AC 13 → MISS`), the NPC turn loop auto-resolving two real hits with correct damage, HP syncing correctly across the HUD and Context Panel, and Flee correctly ending the encounter and reverting to Exploration. Typecheck and build both clean (only the two pre-existing unrelated baseline errors present throughout this whole branch).
- Risk: moderate. This is genuinely new server-authoritative game logic (not just a visual pass) — mitigated by keeping the mechanical model honestly simplified/documented rather than inventing unverified 3.5e math, and by the live seeded-encounter verification exercising the real turn-loop, dice resolution, and WebSocket sync rather than just checking that components render.

## 2026-08-18 (Phase 5)
- In-game UI redesign: implemented Phase 5 (fantasy overlays) of the immersive live-play shell on `feature/immersive-ingame-ui-redesign`, per design spec §12.
- Codex: redesigned as an open-tome two-page spread (Traits/Backstory on the left page, Abilities/other sections on the right) with a spine-gutter shadow on desktop, per spec §12.2's explicit call for stronger book presentation. Content unchanged from Phase 1 — this is a visual treatment pass.
- Inventory: reskinned with leather/wood framing; equipped items get a real visual distinction (amber left border + badge) driven by the actual `equipped` field. No weight/slot/container data was fabricated — encumbrance has no backing data model yet (explicitly Phase 6).
- Character Profile: reskinned as an illuminated portrait-card/manuscript (parchment ground, double bronze border, ornamented portrait frame) per spec §12.5.
- Shop dialog: added a ledger-style gradient header per spec §12.6, without touching `ShopPanel`'s internal logic — the spec explicitly requires preserving native Shop function.
- Character Sheet page (`character-sheet.tsx`): targeted, low-risk pass warming section headers and borders toward the established bronze/ink palette with serif typography, per spec §12.1's "restrained bronze accents" — no rules content, structure, or data touched.
- **Bug fix (pre-existing, found via hands-on verification, not just eyeballing text):** every Dialog-based overlay since Phase 1 (Inventory, Codex, Character Profile, Shop) has been rendering with the wrong background. `game-shell.css`'s surface utilities were written as `.dm-shell .dm-x` (descendant selector), which only matches when `dm-shell` is an ancestor. Radix Dialogs render through a Portal, breaking real DOM ancestry, so every overlay applies `dm-shell` and a surface class to the *same* element — a pattern the descendant selector can never match. The lower-specificity but actually-matching `bg-background` (shadcn's default) was winning by default the entire time. Fixed by adding compound (`.dm-shell.dm-x`, no space) rules alongside the existing descendant ones in `game-shell.css`, covering both the nested-shell case and the portaled-dialog case. Verified via `getComputedStyle` (not just visible text) on all four affected overlays post-fix.
- Verification: typecheck/build pass clean. Browser-verified end-to-end: opened Codex, Inventory, and Character Profile with real character data, confirmed correct background colors via computed styles (not just DOM text), confirmed the character sheet's bronze header gradient renders. Shop chrome could not be live-triggered (no `ANTHROPIC_API_KEY` to have the DM open a shop) but received the same CSS fix verified working on the other three overlays.
- Risk: low-to-moderate. The CSS specificity fix touches shared utility classes used across the whole redesign — verified it doesn't regress the main shell's own (correctly-nested, already-working) usage of the same classes.

## 2026-08-18 (Phase 3)
- In-game UI redesign: implemented Phase 3 (context expansion) of the immersive live-play shell on `feature/immersive-ingame-ui-redesign`, per design spec §8.4.
- World state, for real: `server/dm-engine.ts`'s system prompt already read `campaign.worldState` back into every DM turn, and `server/routes.ts` already had merge logic ready to persist a `[WORLD_STATE]` block — but the AI was never actually told the output format, so the field was silently always empty in practice. Added the missing instruction (mirroring the existing `[SHOP]` example) so `currentScene`, `locations`, `npcs`, `factions`, and `flags` now actually populate during real play.
- `shared/world-state.ts`: type contract + safe parser for the worldState JSON shape, matching the merge logic already in routes.ts exactly.
- Context Panel — Exploration: extracted the inline block into `LocationContext.tsx`, now rendering the real current scene and known NPCs from worldState, with an honest "not yet known" fallback when the DM hasn't established anything (spec §19) — never fabricated.
- Context Panel — Active Conditions: added `ActiveConditions.tsx`, wired to the character's real `active_effects` table via the already-existing `GET /api/characters/:id/effects` endpoint (previously unused by the client). Fixed the `effects_updated` WebSocket handler, which only invalidated `my-character` and silently never refreshed any effects data.
- Context Panel — Loot: added `LootFlash.tsx`, a transient state driven by the real `item_granted` WebSocket event (previously only triggered a refetch; the granted item's own data was discarded). Shows the actual granted item for ~6 seconds, filtered to the player's own character, then reverts to Exploration.
- All three new states are selected by real signals only — an active shop record, a live loot event, persisted worldState/effects — never by matching narration text, per spec §8.4's explicit requirement.
- Deferred, honestly: travel progress and formal quest/objective tracking have no backing data model anywhere in the schema, so no UI was built for them rather than faking a state that doesn't exist.
- Verification: typecheck/build pass clean. Browser-verified end-to-end (desktop + mobile) that the shell renders correctly with the new empty-state Exploration view (no worldState/effects/loot yet — this environment has no `ANTHROPIC_API_KEY` so live AI emission of `[WORLD_STATE]` couldn't be exercised), and that the new effects query fires successfully (200 OK).
- Risk: low. All existing queries/mutations/WebSocket contracts preserved; the effects_updated fix is a genuine bug fix (data was being silently dropped) but scoped to a query that had no consumer before this change.

## 2026-08-18 (Phase 2 — asset acquisition)
- Acquired the 3 scene/texture candidates that were cleared for use with no login/account gate: OpenGameArt's "large parchment texture" (CC0, 1920x1080), OpenGameArt's "Dark Wood, Seamless Handcrafted Texture" (CC0, 2048x2048), and tile 01 of Kenney's 80-tile Pattern Pack (CC0). Each was resized/re-encoded to WebP via a new `script/process-scene-asset.ts` ingestion tool (uses `sharp`, added as a devDependency, dev/build-time only) and committed to `client/public/scene-assets/`; the corresponding `scene-asset-registry.ts` entries now carry real `localAssetPath`, confirmed dimensions, and derivative checksums.
- The other 5 cleared candidates (all DeviantArt: Eerie background with trees and pathway fog, Forest Castle Ruins, Cades Cove Sunrise, Falls Ridge Cave Trail, Blaenavon Ironworks) remain metadata-only — DeviantArt gates native-resolution downloads behind "Log in to download," and creating an account to obtain them was out of scope for this session. Their license terms are still genuinely confirmed (`clearanceStatus: "cleared"`), but with no `localAssetPath` the resolver correctly treats them as not yet selectable.
- The resolver still always falls through to the procedural gradient in the live shell today, since no environment-classification signal exists yet to pick a specific asset out of the pool — that remains correct, not a regression.
- Verification: typecheck/build pass clean; confirmed the new WebP files are served correctly by the dev server at their expected dimensions and that the Credits page renders the updated registry with no errors.
- Risk: low. No raw stock original is exposed or bundled — only the processed derivatives.

## 2026-08-18 (Phase 2)
- In-game UI redesign: implemented Phase 2 (scene/background asset system) of the immersive live-play shell on `feature/immersive-ingame-ui-redesign`, per design spec §13-14 and the 2026-08-17 Scene Background Asset Research Report.
- Scene asset registry: added `shared/scene-assets.ts` (the `SceneAsset` type contract — license, technical, provenance, tags, suitability, clearance status) and `shared/scene-asset-registry.ts`, seeded with metadata for the research report's ~30-item shortlist plus its DeviantArt clearance queue. Licensing is now a property of the asset record, not a comment: nothing is selectable without recorded source URL, creator, license, commercial-use/modification/redistribution permissions, and attribution text.
- Scene resolver: added `shared/scene-resolver.ts` implementing the layered fallback model (explicit location asset → campaign pool → global pool → procedural gradient). Only registry entries marked `cleared` AND with a real, acquired local file may ever be selected; no such file exists yet, so the resolver always falls through to the existing gradient today — that is the correct, honest state, not a placeholder bug.
- Credits page: added `/credits`, a permanent Asset Attributions view listing every registry entry (cleared and pending) with its source, creator, license and required attribution text, linked from the landing page footer.
- Scene treatment: `SceneBackdrop` now accepts per-asset suggested dimming/vignette/blur values from the research report's scene-category treatment table, applied once a real asset is selected.
- No third-party image file was downloaded or bundled in this change. The research report's own clearance checklist requires confirming resolution, checksum and rights at acquisition before any file ships; that acquisition step needs explicit sign-off separately, since downloading external files is a distinct, permission-gated action.
- Verification: typecheck and build both pass clean on the new/changed files (same pre-existing unrelated errors as Phase 1). Re-verified the Phase 1 shell end-to-end in the browser after this change — identical rendering, zero regressions, since the resolver currently always returns the fallback.
- Risk: low. Purely additive (new files + one new route); the one existing file touched beyond registry wiring is `SceneBackdrop.tsx`, extended with optional props that default to Phase 1's exact prior behavior.

## 2026-08-18
- In-game UI redesign: implemented Phase 1 of the approved immersive live-play shell on `feature/immersive-ingame-ui-redesign` — the warm walnut/leather/parchment tavern theme, a responsive three-column desktop shell (Character HUD | Story Chronicle | Context Panel), and a mobile shell with bottom-sheet Character/Context access, replacing the previous manually-laid-out campaign page.
- Character HUD: added a compact always-visible HUD (portrait, HP bar, AC/Init/Speed/Atk-Rnd/Carry, saving throws, currencies, Sheet/Inventory/Codex buttons) driven by a new `RulesAdapter` boundary so the generic shell never hard-codes D&D 3.5e mechanics; any field the current character data can't support renders as visibly unknown rather than being invented.
- Story Chronicle: added a hybrid parchment/leather message feed with distinct system/player/DM styling, replacing the old inline message list.
- Context Panel: added an adaptive panel that switches between an Exploration view (world type, party) and a compact Merchant preview driven by the campaign's real active-shop state, never by narration text matching.
- Contextual actions: added a typing-first action deck (Speak/Look/Interact/Inventory while exploring; Buy/Sell/Ask/Leave at a merchant) that inserts prompt prefixes into the existing composer rather than replacing free-text play.
- Layout preferences: added `useGameLayoutPreferences`, a localStorage-backed hook with Wide/Reading/Cinematic presets controlling HUD/Context column proportions, with a documented seam for future server-backed sync.
- Overlays: migrated the old always-visible sidebar's Inventory and Codex (traits/backstory/abilities/freeform sections) content into dedicated dialog overlays reachable from the HUD, preserving all existing content instead of dropping it.
- Character sheet: consolidated the pre-existing floating "Character Sheet" popup launcher and the new HUD's "Sheet" button onto one shared helper (`openCharacterSheetPopup`) so both open the identical popup instead of growing a second mechanism; removed the now-redundant standalone floating launcher from `App.tsx`.
- Shop: the existing `ShopPanel` component is reused unmodified inside a dialog opened from the Context Panel/action deck, preserving current purchase functionality rather than rebuilding it.
- Bug fix (pre-existing, unrelated to the redesign): `server/auth.ts` was missing `requireDungeonMaster`, `grantDungeonMasterAccess`, and `revokeDungeonMasterAccess`, which `server/routes.ts` already imported and wired to `/api/admin/*` — the server failed to build/boot at all. Added the three functions following this file's existing auth-middleware and `storage.updateUser` patterns.
- Bug fix (pre-existing, unrelated to the redesign): `server/vite.ts` and `server/static.ts` used the Express 5 `"/{*path}"` wildcard syntax for their SPA fallback route, which silently never matches under the installed Express 4/path-to-regexp 0.1.x, so every page request 404'd in both dev and production mode. Replaced with the Express 4-compatible `"*"` wildcard.
- Verification: registered a user, created a campaign and character, and exercised the live redesigned shell end-to-end (desktop 3-column grid at 1440px, mobile single-column with working bottom sheets, Inventory/Codex/Sheet overlays, layout preset persistence) via the running dev server. Live AI-driven flows (Begin Adventure, DM narration, shop granting) were not exercised in this pass — no `ANTHROPIC_API_KEY` was available in the verification environment.
- Deferred to later phases per the design spec: combat UX (§10), full scene/background asset system (§13-14), richer context states (§8.4), and fantasy-overlay redesigns for Character Sheet/Codex/Inventory/Map/Profile/Shop (§12).
- Risk: low-to-moderate. Existing queries, mutations, and WebSocket handling in `campaign.tsx` were left untouched; only the presentation layer and the two unrelated server-startup bugs above were changed. Branch has not been merged to `main`.

## 2026-08-17
- In-game UI redesign: added the approved immersive live-play design specification on `feature/immersive-ingame-ui-redesign`, covering the warm tavern visual system, responsive desktop/mobile shell, compact Character HUD, hybrid story chronicle, adaptive Context Panel, typing-first contextual actions, hybrid tactical combat UX, fantasy overlays, scene-background asset architecture, and licensing requirements.
- Implementation safety: the redesign is explicitly staged so Phase 1 can replace the current gameplay shell without inventing missing rules data or conflicting with the concurrent master item compendium/inventory work.
- Character sheet: added a dedicated read-only D&D 3.5e character sheet popup page that opens separately from the live campaign and leaves all existing campaign bars untouched.
- Character sheet data: added `shared/dnd35-character-sheet.ts` as the canonical structured sheet contract for identity, class levels, ability scores, HP, movement, AC/touch/flat-footed AC, initiative, BAB, grapple, saves, skills, feats, special abilities, proficiencies, languages, weapons, armor, equipment, wealth, encumbrance, spellcasting, physical details, XP, backstory, contacts, enemies, and campaign notes.
- Character sheet projection: existing preset `rulesProfile` data is used as a safe fallback while future character-creation and level-up flows can progressively fill `characterData.dnd35Sheet`; missing values remain visibly unknown instead of being invented.
- Character sheet skills: the popup includes the full core D&D 3.5e skill list with class-skill, trained-only, ranks, ability modifier, miscellaneous modifier, total, and armor-check-penalty columns.
- Character sheet spells: caster class/level, casting ability, save DC by spell level, spells per day, bonus spells, known/prepared spell records, domains, specialization, prohibited schools, and notes are supported without using the repo's unrelated 5e proficiency-bonus cascade.
- Character sheet live data: HP, inventory, equipment, wealth endpoints, feats, abilities, and other recorded character data refresh while the popup remains open; a print-friendly view is included.
- Campaign UI: added a compact `Character Sheet` launcher while inside a campaign. It opens `/#/character-sheet/:campaignId` in a separate resizable browser window and does not replace or resize the existing gameplay layout.
- Characters: added eleven original level-1 D&D 3.5e PHB/SRD starter presets covering all eleven PHB base classes: Fighter, Cleric, Barbarian, Bard, Druid, Monk, Paladin, Ranger, Rogue, Wizard, and Sorcerer.
- Sorcerer: added Neris Tallow, a Half-Elf spontaneous arcane caster with proper level-1 sorcerer spells known, spell slots, saves, equipment, familiar, and progression guidance distinct from the Wizard preset.
- Catalogue: added `shared/default-character-catalogue.ts` as the growing canonical preset export, combining the original ten presets with the Sorcerer while preserving the original array for backwards compatibility.
- Character data: each preset includes core ability scores, HP, speed, AC, initiative, BAB, saves, feats, racial traits, class features, skills, spellcasting where relevant, combat notes, personality, backstory, and starting gear definitions.
- AI progression: each preset carries bounded Claude scaling guidance that preserves the character concept, follows core 3.5e class progression, and flags permanent player choices instead of silently making them.
- Inventory: starter equipment is represented separately from the flexible character-data blob so it can be seeded into the real item system when the character picker is wired into creation.
- Expected impact: DungeonMasterOS now has a reusable starter option for every core 3.5e PHB class and a complete live character-record surface ready to be populated by the character builder and level-up choices.
- Risk: low-to-moderate. The sheet is isolated to a new route/data contract plus a launcher button; no existing gameplay bars or campaign-state contracts were replaced.
## 2026-08-14 (7)
- Public marketing pages: removed all remaining free-trial marketing and old hardcoded prices from the actually-public pages, which the earlier billing UI pass hadn't touched. client/src/pages/landing.tsx (the real "/" homepage — not the same file as the earlier pricing.tsx/billing.tsx work) had a "7-day free trial · No card required" hero badge, "Start Free Trial"/"Start Free — No Card Needed"/"Begin Your Free Trial" CTAs throughout, a trial-only FAQ, and a pricing summary section hardcoding "Weekly £9 · Monthly £30" — old prices that didn't match the OLD model either, let alone the current one. Rewrote all of it, sourcing every displayed price from shared/tiers.ts (TIERS, SQUIRE_PASS, formatPrice) instead of hardcoding strings, so this page can't silently drift out of sync with the real commercial model again. client/src/pages/how-it-works.tsx had the same "Start Free Trial" nav CTA and a "7-day free trial · No credit card required" / "Weekly £9 · Monthly £30" closing CTA — same fix. client/src/pages/auth.tsx's register form said "Begin your adventure — 7-day free trial", "Create Account & Start Trial", and "7-day free trial. No credit card required." — replaced with accurate no-trial copy.
- Risk: none identified. Typecheck clean, 121/121 tests pass (no server logic touched). Manually verified in a browser: homepage, how-it-works, and register pages all render correctly with no trial language and correct current prices (Squire Pass £4.99, Adventurer from £4.99/wk, Master £7.99/wk, Legend £10.99/wk).

- Billing UI (Important + Minor, independent-review findings): billing.tsx and dashboard.tsx both computed their turn-count display from `getEffectiveLimits(...).aiTurnsPerMonth`, which is always the MONTHLY figure — a weekly Adventurer saw "0/200" and was cut off by the server at 50. Both now read the authoritative `turnsIncluded`/`stripeBillingInterval` fields /api/billing already returns correctly (verified in the browser: a manually-set weekly Adventurer test account now shows "12/50" on both pages, not "12/200"). dashboard.tsx's `TurnsUsageBar` also had a latent unlimited-turns display bug (checked `total === 999`, but the API's unlimited sentinel is `-1`) — fixed alongside. Also fixed: a brand-new signup (no trial, `subscriptionStatus:"expired"` by design) was shown the same red "Subscription ended / Resubscribe" banner and badge as an actually-lapsed subscriber, on both dashboard.tsx and billing.tsx — both now distinguish never-subscribed (`expired` + no Stripe customer + no period end) from genuinely lapsed, showing a neutral "Choose a plan" / "Not subscribed" state instead. Minor cleanup done in the same files while touching them: the "Buy More Turns" quick-action and usage-bar CTA pointed at `/billing`, which has nothing to buy since top-ups were disabled — repointed to `/pricing` (Squire Pass); the dead `topup=1` success-toast branch (no code path can produce it anymore) was replaced with the real `squire=1` branch Squire's actual success_url uses; removed now-unused TURN_PACKS/formatPrice/trialDaysRemaining/getEffectiveLimits/TIERS imports from dashboard.tsx.
- Risk: none identified. Typecheck clean, 121/121 tests pass (unaffected, no server logic changed). Manually verified in a browser: fresh signup shows "Choose a plan"/"Not subscribed" instead of the lapsed-subscriber banner; a weekly Adventurer test account shows correct 12/50 turn counts on both billing and dashboard pages.

## 2026-08-14 (5)
- Billing (Important, independent-review findings): fixed two entitlement-gating gaps in server/auth.ts. (1) `unlimitedTurns` was set by `grantDungeonMasterAccess` but never read by `checkTurnLimit` or `requireCanPlay` — a DM-granted account is otherwise an ordinary account (tier stays "free", subscriptionStatus untouched), so it was still blocked: 0 included turns meant the first real `/action` 403'd, and a post-no-trial signup's `subscriptionStatus:"expired"` meant `requireCanPlay` 402'd before that. Both middleware now short-circuit on `req.user.unlimitedTurns`. (2) Squire Pass turns (`bonusTurns`) are sold as one-time, no-expiry AI turns, but `requireCanPlay` blocked `/action`/`/start` entirely once `subscriptionStatus` reached `"expired"` — before `checkTurnLimit` ever got to look at `bonusTurns` — so a user who bought a Squire Pass, subscribed, then let the subscription lapse could never spend the turns they separately paid for, even though `/api/billing` kept reporting them as available. `requireCanPlay` now also lets a user through when `bonusTurns > 0`, leaving `checkTurnLimit`'s existing per-request accounting (0 included turns for a non-playable tier + remaining bonusTurns) to do the real gating. Added 5 tests to server/auth.test.ts, verified RED against the unfixed code for both findings before confirming GREEN.
- Risk: none identified. 121/121 tests pass (117 prior + 4 new), typecheck clean.

## 2026-08-14 (4)
- Billing (Important, independent-review findings): fixed three interrelated bugs in the customer.subscription.updated webhook handler and the Squire Pass active-subscription guard. (1) Portal cancellation reverted to "active": the handler only read sub.status, never sub.cancel_at_period_end — a user who cancelled via the Stripe Billing Portal (still paid through their current period, won't renew) had subscriptionStatus written back to "active", showing a green Active badge right after cancelling. Now stored as "cancelled" when cancel_at_period_end is true, matching what canPlay()/isReadOnly() already expect for that status. (2) usageResetAt not re-anchored on interval change: the handler updated tier/stripeBillingInterval but left usageResetAt on its old schedule — a weekly->monthly upgrade granted a free extra period before the stale weekly anchor caught up; a monthly->weekly downgrade left the user under-provisioned until the stale monthly anchor fired. Now recomputed (with a matching aiTurnsUsedThisMonth reset) only when the interval actually changes. (3) A cancelled-but-still-paid subscriber could buy a Squire Pass and lose their remaining paid period: the guard on POST /api/stripe/squire only blocked "active"/"past_due", not "cancelled" — but per fix (1), "cancelled" can mean still paid through subscriptionCurrentPeriodEnd, and Squire fulfillment unconditionally wipes the subscription linkage fields. Guard now also blocks "cancelled" when subscriptionCurrentPeriodEnd is still in the future; a genuinely lapsed cancelled subscriber (period already ended) is unaffected. Added script/test-billing-webhook-lifecycle.ts (4 tests), verified RED against the unfixed code for all three findings (a temporary non-aborting diagnostic run confirmed tests 1-3 fail at exactly the predicted points, test 4 correctly already passed) before confirming GREEN.
- Risk: none identified. Typecheck clean, all 16 billing scripts pass (15 prior + 1 new).

- Security/Billing (Critical, independent-review finding): POST /api/items/:id/use had no requireAuth, requireCanPlay, or checkTurnLimit — the only AI-invoking route in the file without them. getVisitorId(req) trusted a raw client-supplied `x-visitor-id` header whenever there was no authenticated session, and a logged-in owner's character is stored with `visitorId === "user-<id>"`. An entirely unauthenticated request carrying `x-visitor-id: user-<id>` therefore satisfied the route's only ownership check, and since req.user was undefined, turn metering never ran — unlimited, unmetered, unauthenticated Anthropic API calls against any character whose owning user id could be guessed (sequential autoincrement ids). Fix: added the same `requireAuth, requireCanPlay, checkTurnLimit()` gate every other AI-invoking route already uses, and hardened getVisitorId to refuse an unauthenticated client-supplied header shaped like `user-<id>` (falling back to a random anon id instead) — closing the same impersonation vector for the character read/update/delete routes, which share the same helper. Added server/items-use-auth.test.ts (3 tests), verified RED against the vulnerable code (an unauthenticated spoofed request got 200 instead of 401; a quota-exhausted authenticated user got 200 instead of 403) before confirming GREEN against the fix.
- Risk: none identified. 117/117 tests pass (114 prior + 3 new), typecheck clean.

- Infra: fixed a dead SPA-fallback route in both server/vite.ts (dev) and server/static.ts (production). Both used the Express-5-only `"/{*path}"` wildcard pattern, but this project runs Express 4.22.1 (path-to-regexp 0.1.13), which cannot parse that syntax as a wildcard — the route never matched anything, silently making the fallback dead code in both files. Impact differed by environment: in dev, nothing else serves "/" (no express.static default-index behavior), so every request 404'd, including the app's own root — dev was 100% unusable, which is how this was found (verifying the pricing/billing UI work above required starting the dev server). In production, express.static's built-in index-serving happened to keep "/" itself working, so the live impact was narrower: any bare-path request the static middleware doesn't have a file for (typed URLs, old bookmarks, external links, link-preview scrapers) got a raw "Cannot GET" 404 instead of the SPA shell. This is NOT a live outage for real users: the app routes entirely via wouter's useHashLocation (client/src/App.tsx), and every server-generated redirect (Stripe success/cancel URLs) already uses the "#/..." hash form, which browsers never transmit to the server — real in-app navigation, reloads, and Stripe redirects were unaffected throughout. Fix: dropped the unsupported wildcard argument from both `app.use(...)` calls, matching Express 4's own path-less-middleware convention for a catch-all. Added server/spa-fallback.test.ts, which spawns the real build artifacts (dist/index.cjs for production, tsx server/index.ts for dev — server/static.ts's `__dirname`-based path resolution only resolves correctly from the bundled CJS output, so importing it directly under this project's ESM test harness isn't representative) and verifies: "/" serves the app in both environments, a bare deep-link path falls through to the same shell instead of 404, and a real registered API route is not swallowed by the fallback. Confirmed via revert: reintroducing the old `"/{*path}"` pattern makes both new tests fail at the exact predicted assertions (dev fails on "/" itself; production fails only on the "/pricing" deep-link) before reverting back to the fix.
- Risk: none identified for real user traffic (see above). 114/114 tests pass (112 pre-existing + 2 new), typecheck clean, production build succeeds. Pre-existing gap noted, not introduced by this fix: neither dev nor production registers a dedicated JSON 404 handler for unmatched `/api/*` paths, so a request to a genuinely nonexistent API route falls through to the SPA shell (200 HTML) rather than a proper 404 JSON response — true both before and after this change, left as-is since fixing it is out of scope here.

## 2026-08-14
- Billing UI: closed the billing-reconciliation review's Important finding #1 (client ignoring the new per-interval turn entitlements). shared/tiers.ts: fixed `legend.upgradePrompt`, which still told Legend subscribers hitting their limit to "upgrade to Chronicler" — Chronicler is retired from new sales, and this string is rendered verbatim in two server error messages (server/auth.ts campaign-limit and turn-limit responses) and one client render (billing.tsx). client/src/pages/billing.tsx: removed the "Buy More AI Turns" top-up section (topUpMutation, buyingTopUp state, TURN_PACKS/formatPrice/Gift/useState imports) — it posted to POST /api/stripe/topup, which now always 410s, so existing subscribers clicking Buy Pack hit a dead endpoint. client/src/pages/pricing.tsx: full rewrite — dropped "free" and "chronicler" from TIER_ORDER, removed all free-trial marketing copy and the top-up-discount/free-trial FAQ entries, added a dedicated Squire Pass card wired to POST /api/stripe/squire (not the subscription /api/stripe/checkout mutation), and replaced the static hardcoded TIER_FEATURES turn counts with a dynamic getTierFeatures() that calls getIncludedTurns(tier, interval) so weekly/monthly/yearly each show their real entitlement (e.g. weekly Adventurer now correctly shows "50 AI turns/week" instead of the old static "200 AI turns/month"). Added explicit "turns replenish monthly" wording under the yearly-billed price line so annual subscribers aren't misled into thinking turns are granted once per year.
- Risk: none identified. 112/112 pre-existing tests plus all 15 billing scripts pass, typecheck clean. Manually verified in a browser: pricing page renders all four purchase options with correct dynamic turn counts across all three intervals, Buy Squire Pass posts to /api/stripe/squire, billing page renders correctly for a brand-new (tier=free, status=expired) user with no dead top-up UI.

## 2026-08-14 (earlier)
- Billing: disabled POST /api/stripe/topup — original billing spec's "Item B". The old turn top-up economics are not approved for the current commercial model. New purchases now get an immediate 410 Gone with a clear message and a TOPUP_DISABLED code, before any of the route's old validation (packId presence, pack lookup, tier eligibility, price lookup) or any Stripe API call runs. Historical support was deliberately left untouched: reserveTurnsFor and the checkout.session.completed webhook's topUpTurns fulfillment branch (both used to correctly account for any already-completed old purchase) are unaffected. getTopUpPriceId, whose only caller was this route's now-removed purchase-creation body, was removed as genuinely dead code; the now-unused TURN_PACKS import was also dropped from this file (shared/tiers.ts's own TURN_PACKS/getTopUpPrice exports are untouched, in case a future redesign needs them).
- Risk: none identified. 112/112 pre-existing tests plus all 15 billing scripts pass, typecheck clean.

## 2026-08-13
- Fix: original billing spec's "Item A" — audited checkout.session.completed's recurring-subscription branch and confirmed it updated tier/subscriptionStatus/stripeSubscriptionId/stripePriceId/stripeBillingInterval/subscriptionCurrentPeriodEnd/aiTurnsUsedThisMonth but never established usageResetAt. A brand-new subscriber's usageResetAt starts null (getNewUserBillingState), and attachUser's auto-reset block only runs when usageResetAt is truthy — so a subscriber whose first invoice.payment_succeeded webhook was ever missed or delayed would have had no reset scheduled at all, accumulating usage forever with nothing to bring it back to zero. Now sets usageResetAt via the same getNextUsageResetAt(interval) helper the invoice.payment_succeeded and attachUser paths already use, computed at initial checkout rather than left to a later event. Also tightened the interval fallback from a bare interval || "monthly" (would have accepted any truthy string) to the same weekly/yearly-else-monthly validated pattern used everywhere else in this file.
- Testing: the regression test for this (script/test-billing-subscription-checkout-reset.ts) exercises the real webhook handler through a genuine signed HTTP request, not a unit test in isolation — this branch calls stripe.subscriptions.retrieve before doing anything else, which would otherwise require a real Stripe network call. Patched the Stripe SDK's shared subscriptions-resource prototype (verified experimentally to affect every Stripe client instance created afterward in the same process, including the one server/routes.ts constructs internally) before importing server/routes, so the real handler runs end-to-end with zero network access.
- Risk: none identified. 112/112 pre-existing tests plus all 14 billing scripts pass, typecheck clean.
- Gameplay/billing: added POST /api/stripe/squire — the missing other half of Squire Pass. Fulfillment (the checkout.session.completed webhook branch) already existed and was tested; there was no route to actually create the Checkout Session a user gets sent to. Authenticated, server chooses the price exclusively from its own STRIPE_PRICE_SQUIRE_PASS environment variable (client-supplied price/amount fields are never read at all, proven by a forged-price request reaching the identical rejection an empty request does), Checkout mode is "payment" (never "subscription"), metadata (userId, purchaseType=squire) is entirely server-generated, and it reuses the existing Stripe-customer-reuse pattern already established by /api/stripe/checkout and /api/stripe/topup.
- Policy: a user with a currently-active recurring Stripe subscription is rejected before reaching any Stripe call, with a distinct message telling them to cancel first. Squire's webhook fulfillment unconditionally resets tier/subscriptionStatus/stripeSubscriptionId/stripePriceId/stripeBillingInterval/subscriptionCurrentPeriodEnd to the Squire shape — without this guard, an active paying subscriber buying Squire would silently orphan a subscription Stripe is still billing them for, with no server-side record of it left afterward. A merely stale/cancelled subscription id does not trigger the guard, only subscriptionStatus === "active" does.
- Refactor: the existing Squire webhook fulfillment now reads SQUIRE_PASS.turns instead of a hardcoded 50 in both the bonus-turn grant and its reserve calculation, so checkout creation and fulfillment can no longer silently drift apart on the turn count.
- Risk: none identified. The regression tests (script/test-billing-squire-checkout.ts) deliberately never let a request reach a real Stripe API call — STRIPE_PRICE_SQUIRE_PASS is left unset throughout, so every request that clears the active-subscription guard stops one line later at a "not configured" check instead; the two distinct 400 messages are what prove whether the guard fired, with zero outbound network access anywhere in the file. (An earlier draft of this test suite did make live calls to Stripe's real API using a fake key, discovered from Stripe's own response headers appearing in the failure output — rewritten before being accepted, since this codebase's own established billing-test convention never does that.) 112/112 pre-existing tests plus all 13 (this file makes 13; the prior reconciliation commit's own message undercounted its 12 as 11) billing scripts pass, typecheck clean.
- Reconciliation: brought the pricing/Stripe/Squire billing work — developed directly against the live production server (no git, individually verified there) while this repo was mid-flight on the combat-action-endpoint-wiring initiative — back into version control. The live server's active release and this repo had diverged in two different directions since a shared July baseline: this repo gained combat wiring, the live server gained a new GBP commercial model (no free trial for new users, weekly/monthly/yearly billing intervals, an internal-monthly entitlement reset independent of Stripe's own invoice cadence, a £4.99 one-time Squire Pass granting 50 bonus turns with no recurring subscription, and Chronicler retired from new sales) plus its own 11-script regression suite, all green on the server. Computed the pure billing diff on the live server itself (pre-change backup vs. current) rather than guessing at what had changed, then applied it here: shared/tiers.ts, server/storage.ts and most of server/routes.ts applied as clean patches (zero overlap with combat wiring). server/auth.ts required a manual merge — both efforts had independently touched checkTurnLimit (combat wiring added the dedupAware opt-in bypass factory; billing made the same function's quota check interval-aware) — merged by keeping the dedup-bypass gate at the top and replacing the quota calculation at the bottom with the interval-aware version, and applying the equivalent interval-aware changes to attachUser's auto-reset and incrementTurnCount directly (both were untouched by combat wiring, so no merge conflict there). Removed recordFreeTrialTurnGrant and the now-unused TRIAL_DAYS import, both made genuinely dead by the billing change removing their only call sites.
- Fix: none of the 11 imported billing regression scripts (script/test-billing-*.ts) ran on first attempt in this environment — all but 2 (the pure-function pricing/policy tests) hardcoded a Linux /tmp/... database path, which doesn't resolve on Windows. This is the same class of portability issue this repo's own storage-dedup.test.ts hit and fixed earlier in the combat-wiring initiative; applied the identical fix (os.tmpdir() plus a timestamp suffix) to all 10 affected scripts. One genuine test-vs-interface mismatch also surfaced and was fixed: test-billing-auth.ts called checkTurnLimit(req, res, next) directly, but checkTurnLimit here is a factory (checkTurnLimit(options?) => middleware) since combat wiring's Fix #1 — the three arguments were silently absorbed as an unused options object, so the test's core assertion never actually exercised the turn-limit rejection it claimed to (next() correctly stayed uncalled, but only because nothing ran at all, not because the guard fired) — fixed to checkTurnLimit()(req, res, next), matching how /start is genuinely registered.
- Risk: none identified. All patches were verified to apply cleanly (or merged by hand where they didn't) before being trusted; 112/112 pre-existing tests plus all 11 imported billing scripts pass, typecheck clean. The billing test suite itself is what proves the reconciliation is behaviorally correct, not just textually merged — it was authored and previously verified independently on the live server against the same production logic now living here.
- Security: closed a billing-control bypass in checkTurnLimit found by the combat-action-endpoint-wiring initiative's final whole-initiative review. The clientSubmissionId dedup bypass added earlier (so a legitimate retry of an already-processed /action submission isn't blocked by the turn limit) was applied unconditionally to every route guarded by the middleware, including POST /api/campaigns/:id/start — which makes a real, turn-incrementing AI call and has no downstream dedup to make the bypass safe. An over-quota user could replay any of their own prior /action submission ids in a /start request and get unlimited free AI generations, each one further incrementing their own counter. checkTurnLimit is now an explicit opt-in factory (checkTurnLimit({ dedupAware: true })), applied only to /action; /start uses checkTurnLimit() with the bypass off by default, so any future route added to this middleware is bypass-safe unless it deliberately opts in. Reproduced and confirmed the original exploit is now blocked; added the checkTurnLimit test the design spec called for and that was never written (both directions: bypass intended on /action, must not apply on /start), plus a route-registration-anchored regression test that behaviorally probes the actual middleware instances Express has registered for both routes, so a future edit that silently mis-registers either route is caught even if checkTurnLimit's own logic is untouched.
- Security: closed a same-type-targeting gap in resolveAttack, also found by the same review. An AI-emitted [ATTACK] was validated for target existence and liveness but not target type, so a player character could be made to attack another player character — real roll, real damage, a second user's character reduced to 0 HP and flagged defeated, potentially ending the encounter in "defeat" via friendly fire. resolveAttack now rejects a target of the same type as the attacker. Reproduced the original exploit against real storage and confirmed it's now rejected with the existing invalid_target error, with the intended victim's participant state and the encounter's other fields byte-for-byte unchanged.
- Hardening: capped [COMBAT_START]'s NPC count at 8 (clamped, not rejected — same posture as the existing stat clamping) after the review identified that an AI proposing a large NPC roster had no bound, letting one metered player turn cascade into dozens of synchronous Anthropic calls while holding the campaign mutex for every other player in that campaign.
- Docs: corrected an overstated claim from Task 1's original changelog entry and the design doc. The [SURRENDER] mechanic validates a proposed target's identity, type, and liveness — it does not validate plausibility. The AI can still convert any encounter into a "victory" by naming every living NPC in one surrender proposal; the genuine improvements over the old [COMBAT_END] (PC immunity even under name collision, no reason-text parsing, an audited System message, the deterministic end-check remaining the sole authority over the actual outcome) still stand and are unaffected by this correction.
- Risk: none identified in the final state. This was the closing review of the combat-action-endpoint-wiring initiative (7 tasks, individually reviewed) — the whole-initiative pass found what task-by-task review structurally could not: shared middleware reasoned about only against the task that touched it, and pre-existing code made newly reachable by this initiative without any task owning it. The Critical and both Important fixes above were each reproduced against the reviewer's own empirical exploit before being accepted as fixed, then independently re-reviewed by a fresh pass that reproduced the same exploits again rather than trusting either report. 112/112 tests passing, typecheck clean. Two Important findings (handleFlee not running the NPC cascade after a flee; no automated coverage of WebSocket broadcasts) were deliberately deferred as a follow-up task per the reviewer's own recommendation, rather than folded into this fix cycle — logged in .superpowers/sdd/progress.md for follow-up, not silently dropped.

## 2026-08-12
- Testing: added server/routes-combat.e2e.test.ts — ten end-to-end tests exercising the live handleAction/handleFlee route logic directly (real migrated SQLite database, real storage, real combat-engine/npc-turn/dm-engine code paths), with only the three AI-calling functions substituted via the injectable deps parameter Task 6 added — never the real Anthropic API. Proves, through the actual production endpoint rather than isolated unit tests: [COMBAT_START] creates a real persisted encounter; a player [ATTACK] produces a real logged roll and real HP mutation; an off-turn attack cannot get an illicit roll credited to the acting character; an AI response containing an unrecognized [COMBAT_END]-shaped tag cannot end combat (closing the loop on Task 1's removal — grepped confirmed zero references remain anywhere in server/); a [SURRENDER] naming both a valid NPC and a PC by name only ever mutates the NPC, verified by reading the PC's own participant state directly rather than inferring it from encounter status; duplicate clientSubmissionId requests are safe no-ops; consecutive NPC turns cascade automatically through the real endpoint in one request; and npcTurnMessages in the HTTP response agree exactly with what's actually persisted to the database.
- Fix: no production code changed by this task, but review caught that the SURRENDER e2e test's original assertion (encounter status "ended") gave false assurance — with the test's single-NPC fixture, defeating the NPC alone already ends the encounter via the NPC-only victory check regardless of the PC's state, so the test would still pass even if the PC-protection guard were completely broken. Confirmed empirically (twice, independently) by temporarily removing the guard and observing this e2e test pass unchanged while the correct pre-existing unit test caught the regression. Fixed by asserting directly on the PC's own participant object, looked up via the encounter id captured before the action (not the active-encounter-only lookup, which the fix subagent additionally discovered would itself have been vacuous here, since a SURRENDER always ends this fixture's encounter one way or another) — re-verified to actually fail under the same reintroduced regression before being accepted.
- Risk: none identified. This is the final task of the combat-action-endpoint-wiring initiative — closes it out with 106/106 tests passing, typecheck clean. Two review rounds: the first caught the SURRENDER assertion gap above via empirical regression-testing rather than static reading alone; the second independently re-verified the fix (including re-running the same empirical regression check itself, not trusting either subagent's self-report) and confirmed zero remaining findings.
- Gameplay: wired the full combat engine into the live POST /api/campaigns/:id/action endpoint. When an encounter is active, a validated [SURRENDER] proposal is checked first, then a player attack (only when it's actually their turn — off-turn submissions fall back to plain narration, no error shown). When no encounter is active, [COMBAT_START] can begin one. [CHECK] resolution remains unconditional either way. After any of these, a chain of consecutive NPC turns automatically cascades until control lands on a living PC or the encounter ends, all within the same response. The response gains an additive npcTurnMessages field alongside the existing playerMessage/dmMessage. handleAction/handleFlee are now exported with an injectable AI-dependency parameter, enabling real end-to-end testing without hitting the Anthropic API.
- Fix: two encounter-broadcast gaps found during review, both traced to the task's own initial code rather than an implementer slip. A player's own killing blow previously ended the encounter with zero encounter_updated/encounter_ended broadcast, because the lookup used to find "the encounter to check" filtered on active status and so missed one that had just flipped to ended in the same request. A freshly-started encounter also produced no dedicated broadcast unless the NPC cascade happened to fire as a side effect. Both fixed by tracking the encounter id independent of its live status and re-checking that status, unconditionally, both before and after any cascade. A third inconsistency was also fixed: the persisted message's narration text and its metadata field disagreed on which result (an attack's or a check's) took precedence when both were somehow present in one AI response — now both use the same order.
- Risk: none identified in the final state. This is the highest-integration-risk task in this plan — it modifies the live production action handler — so the review did a genuine line-by-line before/after trace (not a diff skim) confirming every pre-existing broadcast, the fire-and-forget background extraction block, and the catch-block error handling all survived unchanged. 96/96 tests passing, typecheck clean, both fix commits independently re-verified against the actual encounter-status-transition code paths (attack, surrender, flee) rather than accepted on the fix report's word.
- Gameplay: the DM system prompt now documents the mechanical tags it's expected to emit — [CHECK], [COMBAT_START], [ATTACK], [SURRENDER] — in a new numbered MECHANICS section matching the existing [SHOP]/[ABILITIES] convention. Previously the AI was never told these tags existed, so even the already-shipped [CHECK] resolution (Task 6, several tasks ago) was unreachable in real play. When a combat encounter is active, the prompt now also receives live turn context (whose turn it is, valid attack targets) via a new buildCombatContext helper and an additive combatContext parameter on generateDMResponse — every non-combat prompt (the common case) is otherwise unaffected.
- Risk: none identified. 96/96 tests passing, typecheck clean, and every pre-existing line of the system prompt (tone rules, campaign settings, world state, all prior numbered sections) was independently line-diffed against the pre-change version to confirm nothing besides the new section was altered. One Minor cosmetic finding logged for the final review: the non-combat prompt now has an extra blank line at one section boundary — functionally inert for an LLM, not blocking.
- Gameplay: added server/npc-turn.ts's advanceAndResolveTurns — the orchestrator that cascades a chain of consecutive NPC turns automatically (after a player's attack ends their turn, after combat starts if the first initiative slot is an NPC, after a surrender) until control lands back on a living PC or the encounter ends. This is the piece that turns the individually-tested combat engine pieces from earlier tasks into an actual multi-turn combat sequence. Each NPC turn in a cascade still gets its own persisted message and its own broadcast — never collapsed into one combined message — and the valid-target list for each NPC's turn is recomputed fresh from the encounter's current state, so a PC defeated earlier in the same cascade is correctly excluded from a later NPC's targeting.
- Risk: none identified. Loop-termination — the highest-stakes property here, since this runs synchronously inside a live HTTP request once wired in — was independently traced and proven by the reviewer (not accepted on a passing-test basis alone): each NPC turn unconditionally advances the encounter's turn pointer before the orchestrator's next iteration, so the cascade is bounded by the number of participants regardless of the loop's own defense-in-depth guard counter. 93/93 tests passing, typecheck clean.
- Fix: checkTurnLimit now checks for a genuine duplicate submission (via a new storage.getMessageBySubmissionId) before rejecting a request on turn-budget grounds. A legitimate retry — same clientSubmissionId — whose original submission already exhausted the user's monthly turns previously got a hard TURN_LIMIT error instead of the original result; now it transparently returns what already happened, since handleAction's own dedup path never consumes a new turn for it anyway. No behavior change for requests without a clientSubmissionId (including the campaign-start endpoint, which never sends one), and no way to spoof the bypass without a message that already exists for that exact campaign+id.
- Risk: none identified. 87/87 tests passing, typecheck clean. A flaky test surfaced during review (a hardcoded db path + campaign inviteCode collided with leftover files from a prior run on Windows, where better-sqlite3 keeps file handles open past cleanup) was fixed by moving to a unique-per-run tmpdir path, matching this project's existing test convention; verified by actually running the suite twice consecutively with zero manual cleanup, not just re-reading the diff.
- Hardening: added four defensive guards at the combat-engine boundary ahead of it being wired into live traffic. resolveAttack and fleeEncounter now each reject an inactive encounter themselves (rather than relying solely on their callers to pre-filter), before any mutation or dice roll. resolveNpcTurn now throws if invoked when the current turn isn't actually an NPC's — a deliberate loud failure on caller misuse, distinct from the soft error returns used elsewhere for untrusted AI/user input. Added a regression test proving the existing mixed-defeated-and-fled-PCs outcome logic (added in Task 11) is correct — required zero production changes, confirming it already worked.
- Risk: none identified. 86/86 tests passing, typecheck clean, independently re-verified — each guard's exact behavior (throw vs. return, before vs. after mutation) traced line-by-line rather than inferred from a passing test.
- Security: removed [COMBAT_END] entirely — it let the AI mark every currently-living NPC defeated from an unvalidated free-text "reason" string, meaning the AI could single-handedly end any encounter. Replaced with a validated [SURRENDER] tag: the AI proposes specific NPCs by name, and the server independently verifies each one exists, is actually an NPC (never a PC, even under a name collision — two independent type checks), and is still alive before mutating anything. Invalid names in a proposal are silently dropped without rejecting the valid ones. The mutation is explicit and logged via a real System chat message naming exactly who surrendered, built only from the validated names — never from the AI's reason text. Encounter-end afterward still runs through the same deterministic check every other mutation goes through; a surrender has no authority to end combat itself.
- Risk: none identified. This closed a genuine, previously-shipped server-authority hole before it was ever wired into live play. 82/82 tests passing, typecheck clean, independently re-verified with a line-by-line security trace of all 7 validation properties (PC-immunity, defeated/fled rejection, partial-proposal handling, reason-field inertness, explicit logging, deterministic-only termination, inactive-encounter no-op) — zero findings.
- Note (added 2026-08-13, post-final-review): the "a surrender has no authority to end combat itself" claim above describes *mutation* authority, and is true as far as it goes — but it overstates the full picture. [SURRENDER] validates NPC *identity* (exists, is an NPC, is alive), not *plausibility* (no HP threshold, no round count, no server-rolled morale check). The AI still decides which NPCs propose to surrender and can name every living NPC in one proposal, converting any encounter into a `victory` outcome — it just has to enumerate them instead of sending a bare `[COMBAT_END]` tag. The genuine improvements above (PC immunity, no free-text reason parsing, explicit audit-trail logging, deterministic-end-check-retained) all still hold and are unaffected by this note; what changed is narrowing and auditability of the AI's authority over combat-end, not its removal.
- Concurrency: added server/action-mutex.ts's withCampaignLock and wired it around both live combat-mutating routes (POST /api/campaigns/:id/action and POST /api/campaigns/:id/encounter/flee). Requests for the same campaign now queue and run strictly one at a time; different campaigns remain fully concurrent. This is what makes turn-order enforcement actually hold under real concurrent traffic, not just in the single-player happy path — without it, two players submitting at the same instant could both read the same "current turn" state and both proceed.
- Behavior: the action endpoint now accepts an optional clientSubmissionId and uses the existing createMessageIdempotent (Task 5) to make a duplicate submission — a network retry, a double-click — a safe no-op that returns the original result instead of reprocessing the action, re-calling the AI, or double-counting a turn.
- Risk: low. This required extracting two already-live, real production route handlers into named functions to wrap them, so it was independently verified line-by-line against the pre-change file (not just diffed) to confirm zero behavioral drift beyond the two intended changes. 77/77 tests passing, typecheck clean. One pre-existing (not introduced here) edge case flagged for a follow-up: checkTurnLimit runs ahead of the new dedup check in the middleware chain, so a retry can still be rejected with a turn-limit error instead of transparently returning the original result if the user's turn budget was exhausted by the original submission.
- Gameplay: added applySurrenderTag and fleeEncounter to server/combat-engine.ts, plus a new POST /api/campaigns/:id/encounter/flee route. [COMBAT_END] from the AI has exactly one effect, unconditionally: every currently-living NPC is marked defeated (the AI's "reason" text is stored for narration flavor only and is never parsed or branched on) — that mutation then flows through the same deterministic outcome check every other turn resolution uses, so surrender is sugar for "all enemies defeated," not a separate code path. Players can now flee combat via a dedicated endpoint that resolves their own character only (never an arbitrary participant name) and marks them fled, removing them from turn order.
- Fixed a real, previously-existing gap in checkDeterministicEnd (shared since Task 8): it could not distinguish "every remaining PC left because they fled" from "every remaining PC was actually defeated" — both collapsed to a "defeat" outcome. Now: if no PCs remain and at least one was actually defeated, the outcome stays "defeat" (a defeated ally is treated as the more severe state); only when every missing PC left purely by fleeing does the outcome become the new "all_fled". Both of the function's pre-existing regression tests (Task 8's "all PCs defeated ends in defeat," Task 9's "last NPC defeated ends in victory") were independently re-verified to keep their original outcome values.
- Risk: low-medium — this touches shared, high-blast-radius logic used by every combat task since Task 8. 74/74 tests passing, typecheck clean, independently re-verified including hand-tracing all five outcome paths (victory / defeat-all-defeated / defeat-mixed / all_fled / aborted). One gap flagged for the final review: the "some PCs defeated, some fled, zero living" mixed case has no test coverage, though the one-line branch governing it was hand-traced and confirmed correct. Two Minor notes also logged: fleeEncounter has no active-encounter guard (currently harmless — its only caller pre-filters), and the flee route omits the subscription gate present on other mutating routes (likely intentional since fleeing makes no AI call, but not explicitly reasoned about).
- Gameplay: added server/npc-turn.ts's resolveNpcTurn — when it's an NPC's turn in combat, the server asks the AI what it does, but the server always decides the real target: an AI-proposed target is only honored if it names a real, currently living player character; a missing tag, malformed response, invalid/dead/nonexistent target, or an outright AI-call failure all fall through to the same deterministic rule — attack the living PC with the lowest current HP. NPC attacks now resolve through the identical dice math as player attacks (see next entry) so there is exactly one source of truth for combat rolls, not two parallel implementations.
- Refactor: extracted the actual roll/damage/log/HP-mutation/narrate/turn-advance logic out of Task 9's resolvePlayerAttack into a new shared executeAttack, so resolvePlayerAttack now only validates the player's [ATTACK] claim (is it their turn, is the target real/alive) before delegating to the same core NPC attacks use.
- Fixed a real bug uncovered by this refactor, present since Task 9's original commit: resolvePlayerAttack never advanced the encounter's turnIndex before handing off to the turn-loop, meaning (once wired to a live route) combat would never progress past the very first attacker's turn — the same participant would be re-selected as "current" indefinitely unless they happened to die or flee. It went undetected because Task 9's own test file never asserted turnIndex after a normal, non-ending attack. Fixed by advancing turnIndex by one before the turn-loop hand-off, independently re-derived and confirmed correct by the reviewer via direct trace of the turn-loop's own logic, plus cross-checked against Task 8's pre-existing test fixtures which encode the same handoff contract.
- Risk: low, though this is the third task in a row (8, 9, 10) where an implementer independently caught a real bug in the plan's own literal given code around this exact turn-advancement handoff — worth extra attention when Task 11 wires this into the live route handler. 68/68 tests passing, typecheck clean, independently re-verified including a from-scratch re-derivation of the turnIndex fix's correctness (not just its plausibility).
- Gameplay: added resolvePlayerAttack (`resolveAttack`) to server/combat-engine.ts — resolves a player's [ATTACK] proposal entirely against server-held state: validates it's actually the claimed attacker's turn and the target is alive/present, then rolls to-hit against the target's real AC and damage against the attacker's real damage dice/attack bonus (never reading a number the AI supplied). Natural 20 is an automatic hit with doubled damage dice (not a doubled flat modifier); natural 1 is an automatic miss with no damage roll. An invalid turn or invalid target is rejected before any dice are rolled or logged. On a hit, the target's HP is floored at 0 and flagged isDefeated — 0 HP remains incapacitated, never dead, per the fixed v1 policy. Every attack, hit or miss, advances the turn loop via Task 8's advanceToNextActionableTurn, so defeating the last enemy can end the encounter as a direct consequence of this call.
- Risk: low. 63/63 tests passing, independently re-verified by an adversarial reviewer who re-derived both non-obvious fixes from first principles rather than trusting the report: (1) the plan's own "defeats the last NPC" test fixture under-supplied RNG values for a natural-20 critical (which needs 3 draws — 1 to-hit + 2 doubled damage dice — not 2), silently producing NaN damage that left the encounter un-ended; fixed by extending the fixture's RNG sequence. (2) the plan's own given implementation code had a TypeScript compile error (resolveD20's general D20Outcome return type vs. this function's narrower "hit"|"miss"); fixed with a type cast, verified safe by tracing that kind:"attack" can never yield any other outcome value.
- Gameplay: added advanceToNextActionableTurn to server/combat-engine.ts — the turn-advancement loop that makes "the server remains authoritative over turn order" real: automatically skips defeated/fled participants, ticks active effects for every character on round rollover, and checks deterministic victory/defeat conditions after every single turn skip (not just at round boundaries). An already-ended encounter is a true no-op.
- Risk: low. 56/56 tests passing, independently re-verified with hand-traced logic, not just re-run. Two genuine bugs found in this plan's own test fixtures (not the implementation) — both scrutinized carefully given this task's stakes (turn-order integrity) and confirmed as real fixture-design errors, correctly fixed without weakening any original assertion. Noted for later: a fully-fled party currently resolves to a "defeat" outcome rather than "all_fled" — expected for now, addressed when the flee endpoint ships.
- Gameplay: added server/combat-engine.ts's startEncounter — when the DM AI proposes [COMBAT_START], the server now creates a real encounter, seeding player-character combat stats (AC, HP, attack bonus, damage dice) entirely from their actual database rows, clamping any AI-proposed NPC stats against the campaign's powerLevel before persisting them, and rolling+logging initiative for every participant. Not yet wired into live gameplay (that's the turn loop and endpoint integration in the next tasks) — this is the encounter-creation half.
- Risk: low. 49/49 tests passing, independently re-verified. One more real bug caught in the plan's own given code (a storage-interface mismatch identical in shape to Task 6's) and fixed with a Map-backed adapter sourced from the same already-fetched character rows — reviewer traced it back to confirm no fabricated or stale data, and confirmed active-effect modifiers still route to real data rather than being silently dropped.
- Gameplay: wired [CHECK] resolution into the live POST /api/campaigns/:id/action endpoint — the first real mechanical resolution in production gameplay. When the DM AI proposes a check instead of freely narrating, the server now resolves it with real dice math against the player's actual database-backed stats, logs the roll before generating any narration, and only then asks the AI to narrate the already-fixed outcome. Ordinary narration (no check proposed) is completely unaffected — same fallback behavior as before on every existing path.
- Behavior: if the narration AI call fails after a roll has already resolved, the turn falls back to a templated outcome description rather than failing the request — the roll and its result are never lost even if narration prose generation has an outage.
- Risk: low-medium (first change to the live action endpoint). 45/45 tests passing, independently re-verified; the routes.ts edit was confirmed against the actual current file content, not assumed. Two Minor follow-ups logged for the final review (a hardcoded single-character closure in the resolver's internal adapter, and a storage-interface type slightly looser than ideal) — neither affects current behavior.

## 2026-08-11
- Storage: added encounters/rollLog CRUD methods and createMessageIdempotent to server/storage.ts. The idempotent message creation uses the clientSubmissionId unique index from the earlier schema migration to make a duplicate action submission (retry, double-click) a clean no-op that returns the original message instead of double-processing it, including under a genuine race via catch-and-refetch on the unique-constraint violation.
- Risk: low. 41/41 tests passing, verified by independent re-run, no regressions. One minor follow-up noted for later: the idempotency catch block wraps slightly more than the insert itself, which could in a rare case mask an unrelated post-insert failure rather than only a true duplicate-submission race — logged for the final review, not blocking.
- Engine: added server/mechanics-tags.ts — strict JSON-payload parsing/validation for the DM AI's [CHECK]/[COMBAT_START]/[ATTACK]/[COMBAT_END] proposals, extending the existing [SHOP]/[WORLD_STATE] bracket-tag convention. AI-proposed check DCs are clamped to 5-25 rather than rejected (matches the committed design); extractAttackTag discards any extra field the AI includes (a bonus, a damage value) and returns only attacker/target. Every function returns null (never throws) on malformed input.
- Risk: low. 35/35 tests passing (14 dice-engine + 6 character-stats + 15 new, independently re-counted during review), no regressions.
- Engine: added server/character-stats.ts — resolves a character's real, DB-backed modifier for a roll (ability score + active-effect statMods + proficiency bonus), reusing dice-engine.ts's math rather than duplicating it. Skill labels map to their governing ability via the same SKILL_ABILITY convention client/src/lib/computedStats.ts already uses, and the server derives the ability from a recognized skill rather than trusting an AI-supplied ability field.
- Fixed a bug in this same commit (not a pre-existing regression): saving-throw proficiency lookup now correctly matches against full ability names ("Constitution Save") instead of abbreviations ("Con Save"), matching the real proficiencies data shape. A twin of this bug exists in the pre-existing client/src/lib/computedStats.ts and has been flagged separately for its own fix.
- Risk: low. 20/20 tests passing (6 new + 14 regression), no player-facing change yet.
- Engine: added server/dice-engine.ts — pure, dependency-free dice/resolution math (ability modifiers, proficiency bonus table, d20 check/attack/save resolution with real crit/fumble rules, damage rolls with dice-only doubling on a crit and a 1-damage floor, NPC stat clamping by campaign powerLevel, initiative tie-breaking). 14 unit tests, RNG always injected — no Math.random() in the module.
- Behavior: no player-visible change yet — nothing calls this module yet (wired into gameplay starting Task 6). Natural 20 forces success/hit and natural 1 forces failure/miss regardless of modifier, matching the committed design.
- Risk: low. Fully unit-tested pure functions, no I/O, no DB.
- Schema: added the dice/mechanics engine's foundation — six ability score columns (str/dex/con/int/wis/cha), ac, damageDice, attackAbility, and proficiencies on characters; a clientSubmissionId column + unique partial index on messages for duplicate-submission handling; new encounters and rollLog tables.
- Behavior: no player-visible change yet — this is schema only, additive, and idempotent against the existing production database (addColumnIfMissing/CREATE TABLE IF NOT EXISTS, same pattern already used for every prior migration in this file).
- Expected impact: unblocks the rest of the dice/mechanics engine plan (check/attack resolution, combat encounters); ability scores default to 10 (average) for existing characters until the character sheet is wired to set them explicitly.
- Risk: low. Purely additive migration, verified against a live migrated database (PRAGMA table_info / sqlite_master), run twice to confirm idempotency. No existing table or column was modified.

## 2026-04-18
- Routes: applied the uploaded `server/routes.ts` visitor identity patch shape directly to `main` while preserving the rest of the newer route file.
- Behavior: logged-in players resolve to `user-{id}` before any `x-visitor-id` or anonymous fallback is used.
- Expected impact: stable character ownership and campaign identity for signed-in users, which supports Enter the World and character import flows.
- Risk: low. Narrow route-level change only.
- Deployment: added a fresh commit to force Railway to redeploy current `main`.
- Auth/routes: confirmed `server/routes.ts` already uses stable logged-in visitor identity (`user-{id}`) before falling back to `x-visitor-id` or anonymous IDs.
- Expected impact: logged-in users keep a stable character/campaign identity, which unblocks Enter the World and character import flows after redeploy.
- Risk: low. No gameplay logic changes in this commit beyond triggering a fresh deployment of the already-fixed route code.

## 2026-04-17
- Auth: production now fails fast when `JWT_SECRET` is missing instead of silently falling back to the known development secret. This closes a session-forgery risk and makes misconfiguration obvious during deploy/startup.
